import java.util.Scanner;

public class ej1 {
    static void main(String[] args) {
        //Escribe un programa que lea una cadena y cuente cuántas vocales contiene.
        Scanner scanner = new Scanner(System.in);
        System.out.printf("Escribe algo:");
        String cadena = scanner.nextLine().toLowerCase();
        String[] letraCadena = cadena.split("");
        for (int i = 0; i < letraCadena.length; i++) {
            if (letraCadena[i] )
        }
    }
}
